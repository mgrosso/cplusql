#
# new entries should go at the top, just below this comment and be of the format
#
# <date>
# \tcomment
#
# where <date> is the output of the date command (type ESC ! ! date )
# and \t is 4 spaces, not the \t ascii character. 
#

Mon Dec 15 00:29:54 EST 2003
	dummy first ChangeLog entry. This probably wont be used as 
	changes will be managed with version control system.

